CREATE TABLE AUTHOR (
ID NUMBER,
CONSTRAINT AUTHOR_ID PRIMARY KEY (ID),
NAME VARCHAR2(50) NOT NULL,
LAST_NAME VARCHAR2(50) UNIQUE,
COUNTRY VARCHAR2(100) DEFAULT 'ENGLAND',
AGE NUMBER DEFAULT 0,
ARTICLES_COUNT NUMBER CHECK (ARTICLES_COUNT > 10)
);